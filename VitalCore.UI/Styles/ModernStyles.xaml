<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- VitalCore Color Palette - Fluent 2.0 Neon -->
    <Color x:Key="AccentColorNeon">#00FFFF</Color>
    <Color x:Key="SecondaryColorNeon">#8A2BE2</Color>
    <Color x:Key="DangerColorNeon">#FF4500</Color>

    <!-- Semantic Brushes -->
    <SolidColorBrush x:Key="CpuBrush" Color="{StaticResource AccentColorNeon}" />
    <SolidColorBrush x:Key="RamBrush" Color="{StaticResource SecondaryColorNeon}" />
    <SolidColorBrush x:Key="GpuBrush" Color="{StaticResource DangerColorNeon}" />

    <!-- Card Styles with Hover Effects -->
    <Style x:Key="ModernCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource LayerFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="24" />
        <Style.Transitions>
            <TransitionCollection>
                <EntranceThemeTransition />
            </TransitionCollection>
        </Style.Transitions>
    </Style>

    <!-- Typography Presets -->
    <Style x:Key="MetricValueStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="36" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
    </Style>

    <!-- Chart Overrides -->
    <SolidColorBrush x:Key="ChartGridBrush" Color="Gray" Opacity="0.1" />
    
</ResourceDictionary>